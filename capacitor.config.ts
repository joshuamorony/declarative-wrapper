import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'imperative-to-declarative',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
